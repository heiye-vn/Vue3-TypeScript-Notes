function getLength(msg: string){
    console.log(msg.length);
}

getLength('张三丰')

// getLength()

// getLength(1234)

console.log('往后余生，风雪是你，平淡是你，敲每一行代码心里都是你');

console.log('你是CSS，我是DIV，就算我的布局再好，没有你也就没了色彩');

/*
*   tsc 命令常用编译参数
*       --help：显示帮助信息
*       --module：载入扩展模块
*       --target：设置ECMA版本
*       --declaration：额外生成一个 .d.ts 扩展名的文件。
*           如：tsc ts-hw --declaration 会生成ts-hw.ts、ts-hw.d.ts 两个文件
*       --removeComments：删除文件的注释
*       --out：编译多个文件并合并到一个输出的文件
*       --sourcemap：生成一个sourcemap（.map）文件。sourcemap 是一个存储源代码与编译代码对应位置映射的信息文件
*       --module noImplicitAny：在表达式和声明上有隐含的 any 类型时报错
*       --watch 在监视模式下运行编译器。会监视输出文件，在文件变化时重新编译
* */
